'use strict';

const ScenarioData = require('./ScenarioData');
const ScenarioDataInstance = new ScenarioData();

describe("ScenarioData", () => {

    describe('Init', () => {
        it('should define the class', () => {
        });
    });

});